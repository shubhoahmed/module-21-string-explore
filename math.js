const result = Math.pow(2, 2);

console.log(result);

const num1 = 25;
const num2 = 45;
const gap = Math.abs(num1 - num2);
if (gap < 5) {
    console.log('Make Friend')
}
else {
    console.log('keep distance')
}

const number = 12.548;
const result1 = Math.ceil(number);// we can use round ceil floor
console.log(result1);